<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>

	<h4>Titile</h4>
	<%= f.text_field :title %>

	<h4>Body</h4>
	<%= f.text_field :body %>

	<%= f.submit 'Update Book' %>


	<%= link_to "show",book_path %>
	<%= link_to "back",books_path %>

<% end %>